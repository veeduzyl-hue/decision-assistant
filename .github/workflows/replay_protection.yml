name: Replay Protection Gate

on:
  push:
    branches: [main]
  pull_request:

jobs:
  replay-protection:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci
      - run: npm run build
      - run: npx tsx scripts/verify_replay_protection.ts
